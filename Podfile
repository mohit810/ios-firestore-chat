# line below describes the platform and minimum version
platform :ios, '10.0'

target 'Firestore Chat' do
  use_frameworks!

  # Pods for Firestore Chat
  
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'IQKeyboardManagerSwift'

end
